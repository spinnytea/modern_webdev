define([
	'angular',
	'angular-mocks',
], function (
	angular
) {
	return describe('po_ke_type.types.factory', function () {
		beforeEach(angular.mock.module('po_ke_type.types'));
		it('first', function () {});
	}); // end po_ke_type.types.factory
});